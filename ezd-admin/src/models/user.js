export default class User {
    constructor(name, email,accountName, password, balance) {
      this.name = name;
      this.email = email;
      this.accountName = accountName;
      this.password = password;
      this.balance = balance;
    }
  }